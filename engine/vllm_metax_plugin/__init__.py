"""vLLM-MetaX plugin hooks (Phase 2+ integration point)."""

# Future: register metax_kernels custom ops with vllm_metax CustomOp registry.
# See vllm_metax quant_config and model registry on the target server.

VLLM_METAX_PLUGIN_VERSION = "0.1.0"
